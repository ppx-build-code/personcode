server:
  port: 10002
spring:
  application:
    name: micro-dyu-admin
  cloud:
    config:
      label: dev
      profile: test
      uri: http://localhost:8888/
eureka:
  client:
    service-url:
      defaultZone: http://101.34.104.68:8081/eureka
feign:
  hystrix:
    enabled: true
logging:
  level:
    com.netflix:
      debug
