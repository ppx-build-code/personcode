spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ppx-build-code/personcode.git
          search-paths: repo
      name:
        config-server
      label: dev
foo: dyu test config....
